<h1>All Events</h1>

<% if @events.empty? %>
    <p>There are no events</p>
<% else%>
    <ul>
        <% @events.each do |event| %>
            <li><%= event.name %></li>
        <% end %>
    </ul>
<% end %>
